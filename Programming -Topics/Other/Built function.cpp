# Greatest Common Divisor (gcd) : __gcd(a,b)
# Least Common Multiple (LCM) : a*b/__gcd(a,b) or boost::math::lcm (a,b) [ #include <boost/math/common_factor.hpp> ]
# Read a string : getline(cin, str) or cin.getline(str, 20); [20--> int size]
# transform(s1.begin(), s1.end(), s1.begin(), ::tolower);  [Convert whole String to Lowercsae(tolower)/uppercase(toupper)]
# s2.find(s1)!=string::npos [ Using String s1,s2 ]
# string str = to_string(9954); [Convert numerical value to string ]
# stringstream convert("12345"); int n = 0 ; convert >> n ; cout<<n;  [Convert string to numerical value]
# vector<int>w; set<int>k; set<int>::iterator it = k.begin(); copy(w.begin(), w.end(), inserter(k, k.begin())); [copy vector To set]
# s.erase(remove(s.begin(), s.end(), k[i]), s.end()); //characters from a string
# auto it = make_pair(k, j); if(find(p.begin(), p.end(), it) != p.end()) 
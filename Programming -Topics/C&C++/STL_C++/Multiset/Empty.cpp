#include <bits/stdc++.h> 
using namespace std; 
int main() 
{ 
	multiset<int> s; 

	if (!s.empty()) 
		cout << "The multiset is not empty"; 
	else
		cout << "The multiset is empty"; 
	return 0; 
} 
